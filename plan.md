# Plan: prj-9828-task-mugoh79c

**Project:** `prj-9828-task-mugoh79c`  
**Task ID:** `task-mugoh79c`  
**Repo:** `prj-9828-task-mugoh79c`  

## Summary

Декомпозирую задачу «Сделай скрипт для парсинга CSV» на 4 этапа: структура+ридер, валидация+типы, экспорт+тесты, README+smoke-test. Каждый subtask атомарен и проверяем.

## Subtasks

### 1. Создать структуру репозитория и CSV-ридер

- **ID:** `sub-1`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Инициализировать репозиторий prj-9828-task-mugoh79c: создать requirements.txt (pandas), директорию src/, файл src/csv_parser.py с функцией read_csv(path: str | Path) -> pd.DataFrame, которая читает файл, нормализует BOM/кодировку (utf-8-sig, fallback latin-1), trim заголовков. Точка входа — CLI через src/__main__.py: python -m src path/to/file.csv печатает первые 5 строк.
- **Test plan:** git clone + pip install -r requirements.txt + python -m src tests/fixtures/sample.csv выводит 5 строк без BOM в заголовках; sample.csv с CRLF и запятыми внутри кавычек парсится без ошибок.

### 2. Реализовать валидацию схемы и приведение типов

- **ID:** `sub-2`
- **Profile:** `30razrab2`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Расширить csv_parser.py: добавить класс Schema с объявлением колонок (имя, тип: int|float|str|bool|date), функцию validate_and_cast(df, schema) -> (df_clean, errors: list[dict]). Тихие ошибки (пустые строки в int) → errors с row, column, value; coerced=None если strict=False. CLI флаг --schema path/to/schema.json + --strict.
- **Test plan:** pytest tests/test_validation.py зелёный: негативные кейсы (строка в int, плохая дата) дают errors; позитивные приводят тип через pandas.api.types; strict=True бросает SchemaError.
- **Dependencies:** `sub-1`

### 3. Экспорт в JSON/XLSX и unit-тесты

- **ID:** `sub-3`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Добавить экспорт: src/csv_parser.py export(df, path, fmt='json'|'xlsx'|'csv'). Покрыть тестами pytest: test_read.py (3 кейса — utf-8, latin-1, quoted commas), test_validation.py (см. sub-2), test_export.py (round-trip json, xlsx через openpyxl). Цель: coverage >= 85% модуля src/csv_parser.py. Конфиг pytest в pyproject.toml или pytest.ini.
- **Test plan:** pytest -v проходит все тесты; pytest --cov=src --cov-report=term-missing показывает >=85% для csv_parser.py; xlsx-файл открывается через pd.read_excel без warnings.
- **Dependencies:** `sub-2`

### 4. README + smoke-test + подготовка к ревью

- **ID:** `sub-4`
- **Profile:** `30razrab2`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Написать README.md: установка, использование CLI и как API (пример кода с read_csv + Schema). Добавить tests/fixtures/sample.csv + sample_schema.json. Smoke-test: bash scripts/smoke.sh создаёт tmp.csv с 1000 строк, прогоняет python -m src + экспорт в json, проверяет exit 0 и wc -l. Все артефакты готовы к PR-review Айтишником.
- **Test plan:** bash scripts/smoke.sh возвращает exit 0; README содержит блоки Installation / Usage / API; README рендерится на github; git status показывает только ожидаемые новые файлы, ничего лишнего.
- **Dependencies:** `sub-3`


---
*Generated by Hermes Orchestrator*
